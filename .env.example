# Copy to .env and change every value before any shared/staging deploy.
POSTGRES_PASSWORD=postgres_change_me
APP_DB_PASSWORD=app_password_change_me
# Production: point at the separated/managed Postgres instance (India region).
# DATABASE_URL=postgresql+asyncpg://hms_app:...@db.internal:5432/hms
